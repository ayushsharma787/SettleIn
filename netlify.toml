# Netlify deploy config for the Ahlan scrollytelling site (Vite + React SPA).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — serve index.html for any route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
